#!/usr/local/opt/python/bin/python2.7
import sys
from rebuildgrep.walk import Recurse

from sys import argv
print argv
script, regex = argv


test = Recurse(regex)
f = test.find_matching_filenames()
matched = test.find_matched_files(f)
print matched